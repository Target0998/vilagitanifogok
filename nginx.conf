server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    charset utf-8;

    # Serve extensionless URLs as .html (e.g. /rolunk → rolunk.html)
    location / {
        try_files $uri $uri/ $uri.html =404;
    }

    # Proper 404
    error_page 404 /index.html;
}
